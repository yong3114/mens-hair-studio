# V2.0.2 — Google Address Autocomplete 修正版

这版把 Google 官方黑色 `PlaceAutocompleteElement` UI 换成我们自己的 SVR-style 搜索框。

## 为什么换
Google 的内置 autocomplete widget 在 modal / theme 环境里外观和 suggestion dropdown 控制有限。V2.0.2 改用 Google 官方 `AutocompleteSuggestion.fetchAutocompleteSuggestions()` Data API，因此：

- 输入 2 个字以上自动搜索
- Malaysia only
- Johor Bahru location bias
- 自己控制 dropdown，避免被 modal / z-index / overflow 遮住
- 点结果自动保存完整地址、Place ID、经纬度
- Google Maps / Waze 保留
- Desktop / Mobile 同一套顺滑 UI

## 不需要改 Supabase SQL
Database schema 不变。

## Google API
继续使用原本 GitHub Secret：
`VITE_GOOGLE_MAPS_API_KEY`

需要：
- Maps JavaScript API
- Places API
- Places API (New)

## 更新 GitHub
把这版完整项目覆盖本机项目（保留你自己的 `.env`），然后：

```cmd
git add .
git commit -m "Fix Google address autocomplete UI"
git push
```

GitHub Actions 会自动重新 deploy。

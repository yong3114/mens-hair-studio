# V2.0.3 修复说明

修复 Google Places Autocomplete 请求返回 400 的问题。

## 原因
V2.0.2 的 Johor Bahru locationBias 半径设为 70,000 米，但 Google Places Autocomplete Data API 的 circle radius 上限是 50,000 米，因此请求会报：

`Invalid circle.radius. Radius must be between 0 and 50,000 meters.`

## 修复
将 locationBias 半径改为 35,000 米：

- 仍以 Johor Bahru 为搜索偏好中心
- 仍限制 Malaysia (`includedRegionCodes: ['my']`)
- locationBias 只是排序偏好，不会把 Malaysia 其他地区完全排除

不需要重新跑 Supabase SQL，也不需要更换 Google API Key。

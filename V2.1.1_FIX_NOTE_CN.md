# V2.1.1 修复说明

这版只修两个明确问题，不需要重新跑 Supabase SQL。

1. Booking Details 保存时报 `Could not find the 'customers' column of 'appointments' in the schema cache`
   - 原因：编辑预约时把 Supabase join 回来的 `customers` / `profiles` 对象也一起送回 appointments update。
   - 修复：现在只会提交 appointments 真正允许写入的字段。

2. Address 一点进去就自动 dropdown
   - 原因：已有地址会被当成新的 autocomplete query。
   - 修复：已有地址只显示，不搜索；只有用户实际重新输入至少 3 个字符时才弹 Google 建议。

升级：整包覆盖现有项目，再 git add / commit / push。无需 SQL。

# V2.0.3 → V2.1 更新步骤（现有 Yong GitHub / Supabase）

你现在已经有：GitHub Pages、Supabase V2、Yong/Ah Bi Login、Google Address Autocomplete。
所以这次 **不用重建 Supabase、不用换 GitHub Secrets、不用重做 Google API**。

## STEP 1 — 先更新 Supabase

打开 V2.1 folder：

```text
supabase/03_V2_1_WORKFLOW_MIGRATION.sql
```

Ctrl+A → Ctrl+C。

Supabase → SQL Editor → New query → Paste → Run。

成功必须看到：

```text
V2_1_WORKFLOW_MIGRATION_OK
```

如果出现红色 Error，先停止，不要继续 deploy frontend。

## STEP 2 — 覆盖现有本机 GitHub Project

把 V2.1 ZIP 解压。

将解压后的所有文件复制到你现有：

```text
C:\Users\YONG\Desktop\mens-hair-studio-v2-github-edition
```

Windows 询问时选择 Replace。

注意：
- 不要删除 `.git`
- 不要删除你自己的 `.env`
- ZIP 本身没有你的 `.env`，所以正常覆盖不会把 API Key 清掉
- 你现有 `package-lock.json` 可以保留

## STEP 3 — Local quick check（建议）

双击：

```text
01_RUN_LOCAL.bat
```

打开 localhost 后确认 Login / Dashboard 能进。

## STEP 4 — Push GitHub

VS Code Terminal：

```cmd
git status
git add .
git commit -m "V2.1 workflow and calendar rebuild"
git push
```

GitHub Actions 会自动 build + deploy。

## STEP 5 — Production refresh

等 GitHub Actions：

```text
build ✅
deploy ✅
```

正式网站按：

```text
Ctrl + Shift + R
```

## STEP 6 — 只跑一条完整营业流程测试

不要再每个 module 分开乱 test。直接跑一条真实 journey：

1. Lead：只填 WhatsApp display name，不填真实名/电话。
2. Convert Customer：真实姓名留空也可以。
3. Customer → Book appointment。
4. Calendar → Week / Day 查看 booking。
5. Edit booking → 改 Date / Start time。
6. Desktop 把 booking drag 去另一个时间（或手机点 booking 后改时间）。
7. Start service。
8. Complete service → 放 amount / payment / next maintenance。
9. 回 Calendar → booking 应为 Completed。
10. Before / After → 同一个 Service 上传 Before + After → Compare。
11. Payment → 若 Outstanding，可 Mark Paid。
12. 到期 Maintenance → Service → Follow-up 可 Book next visit。

完成这条 journey 才算核心流程真正通。

## FINAL RLS

V2.1 实际用顺以前，继续不要跑：

```text
supabase/02_FINAL_RLS_LOCKDOWN.sql
```

# V2.2 更新步骤

这版不需要重新跑 Supabase SQL。

1. 保留目前 `.env` 和 `.git`。
2. 解压 V2.2 ZIP。
3. 把 V2.2 内全部文件复制覆盖到你现在的 `mens-hair-studio-v2-github-edition` folder。
4. VS Code Terminal：

```bash
git status
git add .
git commit -m "V2.2 mobile UI and workflow polish"
git push
```

5. 等 GitHub Actions：

```text
build ✅
deploy ✅
```

6. 正式网站 Ctrl + Shift + R。

## 重点测试

- Before / After：Gallery / Compare 排版是否正常。
- Calendar：Confirmed/Tentative 显示 Start service；In progress 显示 Continue service；Completed 不再显示 Start。
- 手机：Dashboard → Leads → Customer → Calendar → Back，浏览器返回应回上一层，不应直接退出网站。
- 手机：每次切换主要页面应回到顶部。
- Customer Profile：新增 `Customer view`，用于预览顾客看到的界面。

## Customer view 注意

V2.2 的 Customer view 是 **Admin Preview**，让你先确认 UI 和内容。
它还不是正式顾客 Login。
正式 Customer Login 需要后续增加 customer auth mapping + customer-specific RLS，确认 UI 后再做会更安全。

# V2.3.1 Hotfix

这版只修测试阶段的两个痛点，不改数据库结构。

## 1. Notification 位置
- Desktop 通知弹窗改成固定显示在左侧 Sidebar 的右边，不会再被屏幕边缘切掉。
- Mobile 继续用顶部全宽弹层。
- Notification panel 增加 Clear，可清空当前账号的测试通知。

## 2. 测试数据删除
- Lead：删除时会一起清掉该 Lead 的咨询预约 / consultation 记录，减少 orphan test data。
- Calendar Booking：未开始 / 未完成的 booking 可在 Booking Details 里 Delete。
- Client：Edit Customer 里新增 “Delete test client”，会清理 linked appointments、consultations、services、payments、credit、photos、deal，并释放该顾客占用的 hair system，再删除 originating lead。
- Payments：Payment list 增加 delete test payment。
- Media：原本已有 delete。

> 已经 In Progress / Completed 的 booking 不提供直接 Delete，避免误删已形成服务记录的数据。测试客户整套历史如果要清掉，用 Delete test client。

## 升级
这版没有新增 SQL，不需要 Run migration。直接覆盖项目后 git add / commit / push 即可。

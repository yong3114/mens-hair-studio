# V2.3.3 Bug Audit

这轮除了修弹窗，也一起处理了几个实际流程细节：

1. **Modal / Drawer 位置异常**
   - 根因：弹窗原本渲染在带页面动画的 React tree 内，手机与部分浏览器会出现 fixed positioning / viewport 计算不稳定。
   - 修复：Modal / Drawer 改为 React Portal，直接渲染到 `document.body`。

2. **手机长表单看不完**
   - 修复：使用 `100dvh`、独立可滚动 body、safe area 与键盘 scroll margin。
   - 手机 360×640 与 390×844 已做 headless Chromium layout smoke test。

3. **表单底部按钮挡住最后字段**
   - 修复：手机取消 sticky action overlay，按钮回到表单结尾，保证所有字段都可滚到。

4. **New Lead 一打开手机键盘就弹出，画面被顶歪**
   - 修复：移除 autoFocus。

5. **Booking Type 从 Free Home Consultation 改成 Studio Consultation 后仍保留 Home / Travel Buffer**
   - 修复：Studio Consultation 自动回到 Studio、Travel Before/After = 0。

6. **Customer Profile 的 Consultation booking 会被当作 Service 打开**
   - 修复：根据 booking type 路由到 Consultation / Service 正确流程，并显示正确按钮文案。

7. **Maintenance Due 可能重复 / 已做过新 service 还显示旧 reminder**
   - 修复：只以每位顾客最新 completed service 的 next maintenance date 作为 follow-up 来源。

## 这轮没有动的东西
- Supabase schema
- Google Maps API setup
- Login / RLS
- Hair-system V2.3 core business flow

所以 V2.3.3 **不需要 Run SQL**。

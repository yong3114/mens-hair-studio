# V2.3.3 — 手机表单 / 弹窗体验修正版

这版不改数据库。重点修复 New Lead、New Client、New Booking 等长表单在电脑/手机上的位置与滚动体验。

## 已修复
- Modal / Drawer 改为 Portal 到 document.body，避免页面动画/容器影响 fixed 定位。
- 手机上长表单使用 dynamic viewport (`100dvh`)；Header 固定、内容区独立滚动，最后的按钮一定可以滑到。
- 手机不再用会盖住字段的 sticky form action bar。
- 560px 以下 Page Header 改为上下排，New Lead / New Client / New Booking 按钮全宽。
- New Lead 不再一打开就自动弹键盘，避免画面被顶歪。
- Google Address、textarea、select 等字段在手机键盘打开时保留足够 scroll margin。
- Free Home Consultation 切换回 Studio Consultation 时，会正确恢复 Studio + 0 travel buffer。
- Customer Profile 里的预约动作现在会判断 Consultation / Service，不会把 Consultation 错送进 Service。

## 更新
无需 Run SQL。覆盖项目后：

```cmd
git add .
git commit -m "V2.3.3 mobile overlay and UX fixes"
git push
```

等待 GitHub Actions build / deploy 都绿色后，手机请关掉旧页面后重新打开，或强制刷新。

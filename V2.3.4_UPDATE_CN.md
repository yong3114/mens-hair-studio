# V2.3.4 更新说明 — Leads CRM + In-progress Status

这版针对两个实际使用问题：

1. Consultation / Service 一旦 Start 后，不能安全改回状态。
2. Leads 页面缺少真正的 Follow-up workflow，Edit / Save 体验也不够清楚。

## 必须先 Run SQL

Supabase → SQL Editor → New query → 完整 Run：

`supabase/05_V2_3_4_LEADS_STATUS_FIX.sql`

成功必须看到：

`V2_3_4_LEADS_STATUS_OK`

## In-progress 状态现在怎么处理

Started 并不等于永远锁死。

如果误按 Start，可以：
- Return to booked → Appointment 回到 Confirmed
- Cancel → Appointment / Active Consultation or Service 一起取消
- Continue → 继续真实流程

Completed 仍然不会直接允许改回，因为 Completed 可能已经产生 Payment、Stock Movement、Deal / Client lifecycle 等正式记录。

这些控制在 Calendar Booking Details、Consultation session、Service session 都可以使用。

## Leads 页面重做

- 整行 Lead 可以直接点击打开 Lead Details
- New / Edit 共用清楚的 side drawer
- Save Lead 使用字段白名单，不再把 database metadata 一起送回 Supabase
- Next Follow-up 永远看得到
- Follow-up Due / Overdue 更明显
- 新增 Follow-up History
- Log Follow-up 可以记录：Channel、Result、Note、Next Follow-up Date
- 自动记录 Last Contacted
- Search + Stage filter + Follow-up due filter
- Dashboard 风格 KPI：Open / Due / Consultation booked / Signed
- 可以从 Lead Details 直接 Book Consultation

## 部署

覆盖项目后：

```cmd
git add .
git commit -m "V2.3.4 leads CRM and in-progress status"
git push
```

GitHub Actions 等 `build ✅ / deploy ✅`，然后正式网站强制刷新。

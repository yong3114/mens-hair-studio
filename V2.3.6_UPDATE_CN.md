# V2.3.6 — Mobile Sheet + Searchable Booking + Form UX

这版不改数据库，不需要 Run SQL。

## 修复 / 优化
- Dashboard 在没有 attention item 时不再渲染多余的 `0`。
- Desktop Lead 的 Stage / Interest / Next follow-up 不再溢出卡片。
- 所有 Modal / Drawer 在手机统一为 App-style full-height sheet。
- 手机 sheet 顶部新增 drag handle；在顶部可向下拉关闭，等同 Cancel。
- 手机长表单正文独立滚动；操作按钮 sticky 在底部，键盘打开时仍可使用。
- New Booking 的 Lead / Client picker 改成可搜索：姓名、WhatsApp name、电话、地区。
- 已选对象会显示 selected card，换人时直接搜索，不需要拉很长的 select。
- 小屏幕 360–390px 进一步优化。

## 更新
覆盖旧项目后：

```bash
git add .
git commit -m "V2.3.6 mobile sheets and searchable booking"
git push
```

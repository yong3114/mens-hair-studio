# V2.3 Hair System Core Workflow — 更新步骤

这版不是普通 UI patch，而是把业务逻辑改成真正的 Hair System 销售流程：

Lead → Consultation → Signed / Follow up / Not signed → Client → Installation → Active Client → Maintenance

## 1. 先 Run Migration SQL

Supabase → SQL Editor → New query

完整复制并 Run：

`supabase/04_V2_3_HAIR_SYSTEM_CORE.sql`

成功必须看到：

`V2_3_HAIR_SYSTEM_CORE_OK`

如果没有看到，不要继续 deploy。

## 2. 覆盖本机项目

把 V2.3 ZIP 解压后，全部复制覆盖到你现在的：

`C:\Users\YONG\Desktop\mens-hair-studio-v2-github-edition`

保留原本的 `.git`、`.env`、`package-lock.json`。

## 3. Push GitHub

```cmd
git add .
git commit -m "V2.3 hair system workflow and staff jobs"
git push
```

等 GitHub Actions：

- build ✅
- deploy ✅

然后正式网站 `Ctrl + Shift + R`。

## 4. 第一条完整测试流程

不要分散测试。只走这一圈：

1. Leads → New Lead（只有 WhatsApp Name 也可以）
2. Lead → Book Consultation
3. Calendar → Start Consultation
4. Consultation Outcome → Follow Up（测试一次）
5. 再开 consultation → Signed
6. 填 Hair System specs + Final Price + Deposit
7. Signed 后自动出现 Client
8. Client → Book Installation
9. Calendar → Start Service
10. Complete Installation
11. Client 变成 Active Client
12. Active Client → Book Maintenance

## 5. Staff Job / Notification 测试

Yong 在 Calendar 建一个 appointment，Assigned To 选择 Ah Bi。

Ah Bi login 后：

- Dashboard → My Jobs 会看到这个 job
- 右上角 Bell 会有 New job assigned

改 appointment 时间后，Ah Bi 会收到 Job rescheduled。

目前 V2.3 的通知是 **In-app notification 正式可用**。
Email / WhatsApp 自动发送会在 Automation phase 接正式 provider/API，V2.3 先不假装已经发送。

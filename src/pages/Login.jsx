import React,{useState} from 'react'
import { LockKeyhole } from 'lucide-react'
import { supabase, configured } from '../lib/supabase'
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(!configured)return <div className="login-shell"><div className="login-card setup-card"><div className="brand-mark big">MH</div><span className="eyebrow">SETUP REQUIRED</span><h1>Connect the system first</h1><p>Copy <strong>.env.example</strong> to <strong>.env</strong>, then fill in your Supabase URL, Publishable/Anon key and Google Maps browser key.</p><div className="code-box">VITE_SUPABASE_URL=...<br/>VITE_SUPABASE_ANON_KEY=...<br/>VITE_GOOGLE_MAPS_API_KEY=...</div></div></div>;return <div className="login-shell"><form className="login-card" onSubmit={async e=>{e.preventDefault();setError('');setBusy(true);const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setError(error.message);setBusy(false)}}><div className="brand-mark big">MH</div><span className="eyebrow">PRIVATE STUDIO SYSTEM</span><h1>Welcome back</h1><p>Sign in as Yong or Ah Bi.</p>{error&&<div className="notice danger">{error}</div>}<label className="field"><span className="field-label">Email</span><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><label className="field"><span className="field-label">Password</span><input type="password" required value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></label><button className="btn btn-primary btn-lg" disabled={busy}><LockKeyhole size={18}/>{busy?'Signing in...':'Sign in'}</button></form></div>}

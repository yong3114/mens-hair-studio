import React,{useState} from 'react'
import { Download, Database, MapPin, ShieldCheck, Cloud } from 'lucide-react'
import { exportAll } from '../lib/api'
import { googleMapsApiKey, supabaseUrl } from '../lib/supabase'
export default function Settings({profile}){const[busy,setBusy]=useState(false);const download=async()=>{setBusy(true);try{const data=await exportAll();const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`mens-hair-studio-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url)}finally{setBusy(false)}};return <div className="page"><div className="page-head"><div><span className="eyebrow">SYSTEM</span><h1>More</h1><p>Connection, backup and deployment information.</p></div></div><div className="settings-grid"><section className="panel settings-card"><Cloud/><div><span className="eyebrow">SUPABASE</span><h2>Cloud database</h2><p>{supabaseUrl||'Not configured'}</p><strong>Connected through .env</strong></div></section><section className="panel settings-card"><MapPin/><div><span className="eyebrow">GOOGLE MAPS</span><h2>Address autocomplete</h2><p>{googleMapsApiKey?'API key configured':'API key missing'}</p><strong>{googleMapsApiKey?'Autocomplete ready':'Add VITE_GOOGLE_MAPS_API_KEY'}</strong></div></section><section className="panel settings-card"><ShieldCheck/><div><span className="eyebrow">CURRENT ADMIN</span><h2>{profile?.full_name}</h2><p>{profile?.role}</p><strong>RLS protected</strong></div></section><section className="panel settings-card"><Database/><div><span className="eyebrow">BACKUP</span><h2>Export database JSON</h2><p>Download a readable snapshot of main business tables.</p><button className="btn btn-primary" disabled={busy} onClick={download}><Download size={17}/>{busy?'Preparing...':'Export backup'}</button></div></section></div></div>}
